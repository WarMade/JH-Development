Config = {}

Config.EnableScript = true

-- Key to toggle relaxed style (set to false to disable hotkey)
Config.ToggleKey = 0x57  -- F10 (change to 0 if you want it always on)

-- Only apply to certain vehicle classes? (leave empty for all)
-- Vehicle classes: https://docs.fivem.net/docs/game-references/vehicle-classes/
Config.AllowedClasses = {}  -- example: {8, 9, 11} for bikes, offroad, etc. Empty = all vehicles

-- Driving style value (relaxed/chill)
Config.DrivingStyle = 786603  -- relaxed / normal traffic style (you can experiment with other values)

-- Animation / style intensity (0.0 = normal, lower = more relaxed)
Config.DriverAggressiveness = 0.0